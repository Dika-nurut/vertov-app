/** Back-compatible API import; the implementation is shared with Board quotes. */
export {
  unitsForGenerationModel as unitsForModel,
  type UnitsForGenerationModelInput as UnitsForModelInput,
  type UnitsForGenerationModelResult as UnitsForModelResult,
} from '@seed/shared/generation-request';
